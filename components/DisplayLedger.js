import React from "react";

export default function DisplayLedger({ ledger }) {
  const income = [];
  const payment = [];
  const dailyCashRelease = [];
  // for (entry of ledge) {}

  console.log(ledger);

  return <div></div>;
}
